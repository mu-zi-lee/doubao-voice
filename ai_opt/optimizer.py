"""AI 优化：对即将粘贴的内容做润色/纠错等，文本进 → 优化后文本出。"""


def optimize(text: str) -> str:
    """
    对输入文本做 AI 优化，返回优化后的文本。
    当前为 stub：原样返回。后续可接入豆包/OpenAI 等 API。
    """
    if not text:
        return text
    # Stub：原样返回
    return text
